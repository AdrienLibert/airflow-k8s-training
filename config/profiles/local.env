# Local profile (default). Omit DEPLOY_PROFILE or set DEPLOY_PROFILE=local.

DEPLOY_PROFILE=local
IMAGE_NAME=airflow-k8s-tasks
LOCAL_PLATFORM_REPOSITORY=local/airflow
AIRFLOW_TAG=3.2.2
PUSH_IMAGES=false
